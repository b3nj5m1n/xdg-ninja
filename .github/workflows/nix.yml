name: "Nix Flake Check"
on:
  pull_request:
    paths:
      - '**.nix'
  push:
    paths:
      - '**.nix'
jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: cachix/install-nix-action@v19
    - run: nix flake check
    - run: nix build
