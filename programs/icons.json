{
    "name": "icons",
    "files": [
        {
            "path": "${HOME}/.icons",
            "movable": true,
            "help": "Supported\n\nThe file ${HOME}/.icons can be moved to ${XDG_DATA_HOME}/icons.\n"
        }
    ]
}
