{
    "name": "fonts",
    "files": [
        {
            "path": "${HOME}/.fonts",
            "moveable": true,
            "help": "Supported\n\nThe file ${HOME}/.fonts can be moved to ${XDG_DATA_HOME}/fonts.\n"
        }
    ]
}
