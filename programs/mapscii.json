{
    "name": "mapscii",
    "files": [
        {
            "path": "$HOME/.mapscii",
            "movable": true,
            "help": "Supported\n\nThe file $HOME/.mapscii can be moved to $XDG_CACHE_HOME/mapscii.\n"
        }
    ]
}
