{
    "name": "gdb",
    "files": [
        {
            "path": "${HOME}/.gdbinit",
            "moveable": true,
            "help": "Alias gdb to use custom locations:\n\n```bash\nalias gdb=gdb -n -x $XDG_CONFIG_HOME/gdb/init```\n"
        }
    ]
}
