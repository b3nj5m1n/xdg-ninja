{
    "name": "fontconfig",
    "files": [
        {
            "path": "${HOME}/.fontconfig",
            "movable": true,
            "help": "Supported\n\nThe file ${HOME}/.fontconfig can be moved to ${XDG_DATA_HOME}/fontconfig.\n"
        }
    ]
}
